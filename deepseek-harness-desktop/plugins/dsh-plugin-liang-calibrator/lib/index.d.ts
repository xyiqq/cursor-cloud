/**
 * dsh-plugin-liang-calibrator — host half type surface.
 */
export declare const name: "liang-calibrator";
export declare const inject: ["webServer"];
export declare function apply(ctx: { webServer: unknown }): void;
